name: Android CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Make gradlew executable
        run: chmod +x ./gradlew
        
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v3

      - name: Build
        run: ./gradlew assembleDebug --warning-mode=all
        
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      #- name: Download Gradle wrapper
       # run: |
       #   ./gradlew --version

      - name: Cache Gradle & Maven
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            ~/.m2/repository
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/gradle-wrapper.properties', '**/*.gradle*', 'gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build (assembleDebug)
        run: ./gradlew --no-daemon assembleDebug --warning-mode=all

      - name: Upload debug APKs
        uses: actions/upload-artifact@v5
        with:
          name: apks
          path: app/build/outputs/**/*.apk
